# 變更紀錄

本專案採語意化版本（Semantic Versioning）。`0.x` 為 1.0 前之迭代階段。
各版本依「新增 / 改善 / 修正」分類條列。

## 未發佈
### 新增
- 事後重新辨識新增最高準度模型 FireRedASR：中文辨識最準，適合會後精校（首次使用會自動下載模型、以 CPU 運算故較慢）。
- 摘要前自動保守校正逐字稿：修正明顯的同音／錯字，並把提到的人名對齊已辨識的聲紋名單（不改數字日期、不杜撰）；只影響摘要、逐字稿原文不動，設定頁可關閉。
- 原生浮動面板即時字幕：邊講邊出現暫定字幕、講完定稿，不再整段跳出（串流體驗）。
- 設定頁可挑選預設即時辨識模型，原生浮動面板與網頁錄音都會套用。
- 語者頁新增聲紋拆分：同一個名字底下混到兩個人時，抽樣重新比對並分成兩群各給一段試聽，你聽完確認即可拆開，之後會自動分辨兩人（執行前自動備份，可還原）。
### 修正
- 「可能是同一人」不再拿兩個已命名的人互相比較建議合併（你已命名＝已確認是不同人）；只保留「未命名語音 ↔ 某人」的建議。
- 「一鍵重新比對」現在會清掉語者庫裡所有未命名的暫存聲紋，讓庫只留有名字的人（更乾淨、辨識更穩）。
- 修正「一鍵重新比對」在合併同名語者時偶爾整個失敗（500）的問題，現在能正常清理聲紋庫。
- 修正 FireRed 校正版逐字稿會冒出 `<sil>` 等雜訊符號（靜音片段整行變成一串符號）的問題，改為自動清除。
- 遠端會議檢視頁改版：播放錄音時會自動highlight目前正在講的那一句、點句子可跳到該時間點，並整體美化排版（含深色模式）。
- 修正即時辨識偶爾中途停止出字、且不會自行恢復的問題（辨識引擎卡住時加入逾時保護，跳過卡住的片段後繼續，音訊仍完整保留可事後重新辨識）。
- 修正上一版把語者辨識標準調太嚴，導致即時辨識認不出已命名者（幾乎都變「對方／說話者N」）的問題，還原為原本能正常認人的靈敏度。
### 改善
- 逐字稿可「抽離」單行：點說話者、勾「只改此行」即可把辨識錯的那一句改成正確的人，其餘不動（例如某句被標成 Imp 其實是 Angle）。
- 辨識不出的聲音不再自動塞進語者庫：未認出者只用會議內部編號（對方1、對方2…），只有當你「指派真名」時才把該聲紋存入全域語者庫。根治語者庫被大量無名聲紋灌爆、拖累辨識的問題。
- 即時錄音的聲紋比對改用 Neural Engine（ANE），降低 CPU 負擔、更省電（辨識準度不變；失敗自動退回 CPU）。
- 背景服務重新啟動時不再中斷錄音：斷線期間持續錄音，重新連上後自動補回這段音訊與逐字稿。
- 按下「完成會議」後自動壓縮錄音檔，佔用空間大幅縮小（數小時錄音約可省下 9 成），播放與重新辨識照常。

## 0.13.2
### 修正
- 大幅改善會議摘要品質：修正摘要有時會偏離實際會議內容、產生亂碼或亂編字詞的問題，現在摘要會確實依據逐字稿內容整理。
- 修正即時辨識有時把不同的人都認成同一位已命名者的問題（提高辨識標準，不再輕易認錯人）。
- 減少即時字幕偶爾冒出一句不相關的話（靜音或雜訊被誤辨識成句子）。
- 即時字幕在一段時間沒人說話後會自動淡出，不再卡住上一句。

## 0.13.1
### 修正
- 修正 v0.13.0 的「自動重連」其實從未生效：面板偵測到「伺服器未連線」時，現在真的會自動重新啟動背景服務並恢復。
- 會議詳情頁改名說話者時，若不小心改成已存在的另一位人員，會先提醒確認；真的要合併時也會自動先備份，避免誤併後無法分離。

## 0.13.0
### 新增
- 語者管理頁新增「🔁 一鍵重新比對」：自動合併非常相近、疑似同一人的聲紋，並清掉只出現一次、從未被認出的雜訊聲紋。
### 修正
- 大幅改善說話者辨識：同一個人不再一直被當成新的人分開記錄（改用每人整合後的乾淨聲紋比對）。
- 「一鍵重新比對」現在也會把同一個未命名者的多筆聲紋併起來；並在頁面標明它會自動處理明顯的、其餘不確定的才列給你判斷。
- 修正會後重新辨識後，逐字稿時間軸與播放位置對不上的問題（同一段落多句擠在同一時間點）。重新辨識後即正確。
- 修正會議詳情頁點擊「對方/我」改名沒有反應的問題：即時辨識還沒命名的說話者，點擊改名現在能正確生效。
- 修正點開 App 後，面板偶爾會一直顯示「伺服器未連線」且永遠恢復不了；現在偵測到連不上會自動嘗試重新啟動背景服務。

## 0.12.0
### 新增
- 錄音面板新增「字幕浮層」開關：在畫面底部以大字顯示最新逐字，像 YouTube 即時字幕，可隨時開關。
### 改善
- 大幅簡化全新電腦的安裝：首次啟動改用內建的一體化 Python 執行環境（含加速套件），不用再裝 Xcode 工具、也不會卡在網路安裝一堆套件。
- 修正「檢查更新」在舊版本上更新不完整的問題：現在會整個 App 一起換新並自動重開，不再只換到一半。
- 麥克風軌也做聲紋辨識：同一支麥克風收多人時，已命名的人也會顯示真名（認不出仍標「我」）。
- 修正麥克風音量過大時削波（爆音），改善麥克風的辨識與轉錄準確度。
- 即時說話者辨識：認出某人後，會自動回頭把他先前顯示「對方」的句子改成真名，不必等會後重新分群。
- 減少同一人短句偶爾閃回「對方」：聲音夠接近前一位已認出的講者時會延續同一人。

## 0.11.0
### 新增
- 開啟 App 直接進入原生錄音面板，不再跳轉瀏覽器。
- 浮動面板改為 Dock 原生 App：Dock 顯示 MeetingSummary 圖示；關窗只縮回 Dock，點 Dock 圖示即可重新開啟。
- 錄音面板顯示即時全文逐字（可捲動），並新增「主控台」按鈕開啟會議清單／摘要／設定。
- 原生面板錄音一律由 App 自身擷取，麥克風／系統音權限歸 MeetingSummary。
- 錄音面板指示燈分三態：待機（灰）、準備中（灰色呼吸燈）、錄音中（紅色呼吸燈），一眼看出是否真的開始收音。
### 改善
- 即時逐字預覽恢復更快的更新節奏，講話時字幕更即時（先前偏慢）。
- 即時說話者辨識更準：每句話比對一次聲紋，並過濾混合軌雜訊、把同一人的多筆聲紋自動合併去雜，減少認錯與漏認。
- 統一即時逐字稿輸出：每行時間一律顯示當下時鐘；尚未認出時標聲道「對方／我」，認出已命名的人才換真名（不再出現「說話者1」）。
- 網頁版錄「對方」聲音改用瀏覽器分頁／畫面音訊分享，免安裝任何原生元件；錄音架構簡化為網頁／原生兩套各自獨立的前端，更穩定。
### 修正
- 修正原生面板錄「系統音（對方）」只錄到一片靜音：改為請求「螢幕與系統音訊錄製」權限，未授權或收不到聲音會明確提示，不再默默錄空白。
- 修正原生錄音容易中斷：連線斷掉會自動重連並接回同一場會議，不再中途停掉。
- 修正系統音偶爾「啟動失敗」。
- 修正關閉浮動面板後叫不回來：點 Dock 圖示（或主控台「🪟 浮動面板」）即可重新開啟。
- 修正錄系統音時，實際聽到的聲音會有「波波」雜訊。

## 0.10.0
### 修正
- 修正原生麥克風錄音沒聲音／音量太小，導致辨識變差的問題（現自動拉回正常音量）。
- 修正原生系統音擷取錄不到對方聲音。
- 修正錄音沒收到聲音時無法停止（原本只能強制關閉程式）。
- 修正按「開始錄音」偶爾沒反應、卡住不動。
- 系統音擷取中途中斷時自動重啟，不再無聲失效。
- 清除殘留的錄音程序，避免占用麥克風、影響下一次錄音。
### 新增
- 沒有麥克風／螢幕錄製權限時會即時提醒，並可一鍵開啟系統設定。
- 可在設定記住預設錄音來源與系統音擷取方式，不必每次重選。
### 改善
- 改善多人交錯講話時的說話者辨識（減少兩人被合併、或一人被拆成多人）。

## 0.9.1
- 修正閒置時 CPU 佔用飆高、越用越燙的問題（資料庫多執行緒存取缺陷）。
- 錄音頁重新整理不再遺失狀態：錄音進行中會自動接回（計時與逐字稿照常顯示）。
- 修正浮動面板重複開啟：已有面板時再按只會沿用原視窗。

## 0.9.0
### 新增
- 浮動面板可直接開始錄音（麥克風／系統音／兩者），不必再開瀏覽器分頁。
- 浮動面板可選錄音來源，並顯示最近幾句即時字幕。
### 改善
- 錄音頁更清爽：開始／停止按鈕更醒目，錄音中顯示計時、紅點與簡易音量指示；分享視窗提示只在需要時出現。
- 會議頁版面調整：摘要在前、回放緊貼逐字稿、截圖移到最後，閱讀動線更順。
### 修正
- 即時聲紋辨識：長語句不再被切出一堆新「說話者N」，已命名聲紋的辨識率也提升。
- 「兩者(混合)」錄音時電腦音訊斷斷續續。

## 0.8.22
- 修正按停止後無法立即在同一個 session 再次開始錄音。
- 即時辨識可認出先前命名過的聲音，直接顯示真名（其餘仍標「說話者N」）。
- 對方即時分群：同一段落多人輪流講話時，會分行標出各自的說話者。
- 系統音若已授權原生擷取，錄音頁自動啟用，不再每次跳瀏覽器分享框。

## 0.8.21
- 修正背景服務偶爾重複啟動互搶，導致網頁時開時不開。

## 0.8.20
- 修正點 App 後有時打不開網頁。

## 0.8.19
- 內部結構整理，提升可維護性（功能不變）。

## 0.8.18
- 修正用系統音原生擷取時容易卡住、按停止也停不掉。

## 0.8.17
- 修正點 App 時 Dock 圖示彈跳很久、有時開不起來。

## 0.8.16
- 修正前一次未正常關閉時，再點 App 會開不了。

## 0.8.15
- 省電（ANE）即時辨識也能邊說邊跳字幕，不必等整句講完。

## 0.8.14
- 修正系統音較長時辨識失敗。

## 0.8.13
- 修正多次錄音後「按停止卻沒真的停止」、介面卡住。

## 0.8.12
- 浮動面板改為原生介面，更精緻。
- 修正螢幕錄製權限清單顯示為「python」的問題。

## 0.8.11
- 浮動面板可看會議標題、即時字幕、計時，並隨手記筆記。
- 錄音頁精簡：常用項目在前，其餘收進「進階選項」，更清爽。
- 系統音原生擷取：會自動跳出螢幕錄製權限請求。

## 0.8.10
- 修正浮動面板關閉後無法再次開啟。

## 0.8.9
- 系統音原生擷取未授權時，會自動跳出螢幕錄製權限請求並給予指引。

## 0.8.8
- 省電（ANE）版本第一次錄音不再久候（背景預先載入）。

## 0.8.7
- 穩定性修正：即時辨識卡住時自動恢復。

## 0.8.6
- 內部穩定性修正。

## 0.8.5
- 修正自訂連接埠失效，以及極端情況下的重複啟動問題。

## 0.8.4
- 修正快速連點「開始錄音」造成的重複錄音；數項小修正。

## 0.8.3
- 多項安全性與穩定性修正（含上傳檔名處理、摘要防杜撰）。

## 0.8.2
### 修正
- 標籤自動補全清單未顯示：輸入標籤時不再提示先前使用過的標籤，現已修正。

## 0.8.1
### 修正
- 原生系統音擷取無法錄到聲音：即時錄音的原生擷取分支誤用未匯入的模組參照，導致擷取程序從未啟動。已將 `backends` 改為模組層級匯入，並修正同源的浮動面板啟動端點，消除此類未定義參照風險。
### 新增
- 設定新增「錄音時自動開啟原生懸浮控制面板」：開始錄音（含手動與快速錄音）時自動開啟原生面板，免手動啟動。
### 改善
- 封裝流程：原生輔助程式（`audiocap`、`floatpanel`）改由 CI 預先編譯並隨發佈附加，使用者端一律下載、不在本機編譯；`setup_runtime.sh` 新增對應安裝目標，設定頁可一鍵安裝。
- 首頁新增「浮動面板」啟動按鈕，可由伺服器直接開啟原生控制面板。

## 0.8.0
### 新增
- 原生浮動控制面板（`swift/floatpanel`）：常駐置頂視窗，可於任一應用程式（Teams、Zoom 等）之上顯示錄音狀態與計時，並提供「開始」「停止」操作（停止由伺服器端結束錄音並完成）。依 `MEETING_PORT`（預設 8765）連線。
- 伺服器端點 `/live/state`、`/live/stop`，供面板查詢狀態與遠端停止。

## 0.7.0
### 新增
- 原生系統音擷取（ScreenCaptureKit）：於錄音頁啟用後，由應用程式以 macOS 原生 API 擷取系統音（對方），免除每次錄音的瀏覽器畫面分享對話框。來源選「系統音」為純對方；選「兩者」為麥克風加原生系統音。
- 說明：首次使用須於「系統設定 → 隱私權與安全性 → 螢幕錄製」授權本應用程式。

## 0.6.1
### 改善
- 介面調整為更明確的 Apple 風格：採用系統灰階（淺色 #f5f5f7、深色純黑）、放大主標題、主要按鈕改為膠囊狀、頂部導覽列改為毛玻璃半透明。
### 修正
- 深色模式下筆記輸入框顯示為白底（多行輸入框未套用深色樣式）。

## 0.6.0
### 改善
- 統一設計系統：精修圓角、陰影層次、字級階層與按鈕互動回饋，全頁面一致。
- 快速錄音改為先顯示選項面板，可選擇來源（麥克風／系統音／兩者）、即時模型與純錄音，並支援雙軌擷取。
### 修正
- 錄音頁閒置（尚無字幕）時出現的黑色字幕區塊。

## 0.5.5
### 修正
- 截圖功能改由瀏覽器擷取畫面後上傳：macOS 螢幕錄製權限綁定應用程式，背景伺服器無法執行 `screencapture`。改用瀏覽器既有的畫面分享權限，免額外授權。

## 0.5.4
### 新增
- 截圖功能：會議頁可擷取整個螢幕（如投影片）附加至該會議，提供縮圖檢視與刪除。

## 0.5.3
### 新增
- 設定頁「資料管理」：顯示總用量與分類大小（錄音資料、資料庫、語音模型、MLX 模型），並列出佔用空間最多的會議以利清理。

## 0.5.2
### 新增
- 任意頁面快速錄音：非錄音頁的右下角提供浮動按鈕，可直接以麥克風開始錄音並即時辨識，錄音中顯示計時與停止。

## 0.5.1
### 新增
- 全頁面錄音指示：錄音進行中時，所有頁面頂部顯示錄音狀態，點選即回到錄音頁。

## 0.5.0
### 新增
- 純錄音模式：僅儲存音訊、不執行即時辨識（零推論、不發熱），適用於裝置過熱或需快速側錄之情境，事後再行語音辨識。可於錄音進行中切換，無須停止重啟。
### 改善
- 降噪開關移至錄音頁，並支援錄音進行中切換即時模型、純錄音與降噪。

## 0.4.9
### 新增
- 語者合併建議新增「不是同一人」：可否決誤判配對，被否決之組合不再出現於建議清單。

## 0.4.8
### 新增
- 標籤與語者名稱輸入自動補全：標籤提示過去使用過之項目；於回顧頁修改語者名稱時，改以內嵌輸入框搭配已知語者清單選取。

## 0.4.7
### 修正
- 語者頁顯示零筆紀錄之孤立語者：無任何逐字稿引用之聲紋（多由重新辨識後遺留之暫定名稱造成）不再列出。

## 0.4.6
### 修正
- 語者合併建議排除無可試聽片段之語者：無有效發言片段（短於 800 毫秒）之聲紋無法以聽覺確認，不再納入比對。

## 0.4.5
### 新增
- 現場筆記作為摘要可信來源：錄音頁與會議頁可即時記錄重點（自動儲存）；產生摘要時，筆記以可信來源提供模型優先採用其中之人名、日期與決議，且筆記內容不受防虛構機制誤刪。

## 0.4.4
### 新增
- 嘈雜環境降噪（實驗性，DeepFilterNet3／Neural Engine）：辨識前先行去除背景噪音，於上傳辨識與重新辨識時生效。預設關閉——實測對乾淨音訊反而增加辨識錯誤並延長耗時，僅適用於確有噪音之錄音。

## 0.4.3
### 修正
- 即時 ANE 辨識無輸出且 GPU 仍運作：(1) 選用 ANE 模型時即背景預載（模型首次載入約 13 秒），避免短錄音來不及產生結果；(2) ANE 即時模式略過 MLX 即時預覽（即時期間之主要 GPU 來源），改為斷句時以 ANE 產生最終結果，大幅降低 GPU 使用（代價為無逐字即時預覽）。

## 0.4.2
- **修正「ANE 還在用 GPU、沒省電」**：speech-swift 的 CoreML **encoder 預設 `.all`** → CoreML 實際把它排在 **GPU**(所以開了 ANE GPU 還在跑)。改成設環境變數 `SPEECH_COREML_COMPUTE_UNITS=ane` 強制 encoder＋decoder 都走 **Neural Engine**(批次 ANE 與 live helper 子行程都設,執行時讀取、免重編)。首次會為 ANE 重新編譯一次 CoreML(之後快取)。註:mel 前處理等 MLX 膠水仍會用到少量 GPU;live 的 interim 預覽仍走輕量 MLX whisper-small(GPU 負擔小)— 要完全離 GPU 可把 interim 也導到 ANE 或關掉,再說。

## 0.4.1
- **修正啟動畫面卡在「啟動中」(其實已啟動)**：bootstrap 啟動splash 原本要先從 `/_setup` 收到 `done=true` 才去檢查 `/health` 重載;但 setup 完成後 bootstrap server 關閉、交棒給真正的伺服器，此時 `/_setup` 失敗 → `_done` 永遠不為真 → 卡在「啟動中」雖然伺服器已起來。改成**每次輪詢先探 `/health`**，真伺服器一接手回 JSON 就重載(交棒競態修掉)。

## 0.4.0
- **Live 即時辨識支援 ANE（省電、離 GPU）**：新增常駐 Swift helper `qwen3-ane`（speech-swift Qwen3-ASR CoreML）— 載入一次模型在 Neural Engine，伺服器把每個 live 視窗經 stdin/stdout 餵給它（實測載一次後 warm ~RTF 0.15）。Live 模型下拉在「M 系列 + 已安裝 helper + ANE 開關」時多出「Qwen3-ASR(ANE·省電)」，選了即走 ANE、不燒 GPU。
  - **預編譯發佈**：release CI 編好 helper（含 `mlx.metallib`，runner 有 Xcode metal toolchain）並附上 `qwen3-ane-arm64.tar.gz`；「設定 → 加速 runtime」的 `qwen3-ane` 一鍵下載安裝（免 Xcode），下載不到才退回本機編譯（需 Xcode）。binary + metallib 可攜（僅依系統 framework）。
  - 註：live interim 仍走輕量 MLX whisper-small（GPU 負擔極小）；ANE 用於 live 的 final/accurate 與批次。

## 0.3.4
- **重新辨識逐字稿改為句子級(像 live，不再一大段)**：qwen3/ANE 一個 30 秒視窗原本回傳一整塊文字、存成一行很難讀。`asr.transcribe` 現在把每段依標點(。！？；)切成句子級的多行、時間依字數比例分配；短句／無標點(如 whisper 本來就斷好的)維持原樣不會被過切。批次重辨識與上傳辨識都套用。

## 0.3.3
- **模型依後端分組，方便辨識**：即時模型、重新辨識的下拉用 `optgroup` 分成 **🧠 NPU·ANE 省電／⚡ MLX·Metal/GPU／🔧 .cpp·Metal／🐢 transformers·慢**；設定頁「支援的模型」表也加上分組標題。一眼看出每個模型跑在哪個後端（GPU 還是 Neural Engine）。

## 0.3.2
- **ANE `speech` CLI 改為 app 內一鍵安裝**：不用再自己開終端機 `brew install speech`。「設定 → 加速 runtime」多出 `speech`(ANE 省電辨識)一列，按「編譯安裝」即在背景 `brew install speech`(含進度)。M 系列若尚未安裝，實驗性功能區的 ANE 說明會指向那裡;裝好後開關自動出現。`setup_runtime.sh` 加 `speech` 目標(冪等)＋確保非登入 shell 也找得到 brew。

## 0.3.1
- **ANE 省電辨識納入自動流程**：開關開啟後（M 系列），不只手動下拉選 ANE — **上傳音檔的自動辨識**、以及**未指定模型的「重新語音辨識」** 都改走 Neural Engine（離 GPU、省電）。新增 `_default_asr()` 統一決定自動/預設用哪個 backend；上傳 (`/ingest`) 與兩條 transcribe 路由共用。關閉開關則回到原本 MLX 預設。

## 0.3.0
- **實驗性：ANE 省電辨識（Apple Neural Engine·M 系列）**：重新辨識可改跑在 Neural Engine，**完全離開 GPU → 省電、不發熱**（先前 ASR 跑 GPU 會過熱）。
  - 新增 `ane` ASR backend：透過 homebrew `speech` CLI（Qwen3-ASR CoreML）。把音訊切成 30s 窗丟進暫存資料夾、`transcribe-batch` 一次跑完（模型只載一次），實測 **RTF 0.039**，輸出自動轉繁體。zh-TW 逐字稿與現有 MLX 路徑幾乎一致（CoreML INT8 量化，WER 略高於 MLX 8-bit，但內容感受不大）。
  - **Gated**：偵測到 Apple Silicon + `speech` CLI 才在「設定 → 實驗性功能」顯示開關；開啟後「重新語音辨識」下拉多出 Qwen3-ASR(ANE/混合) 選項。需先 `brew install speech`（homebrew-core）。
  - 註：實測 sherpa 分群走 CoreML 反而更慢（onnx ops 退回 CPU），且分群本來就跑 CPU、非發熱源 → 不採用，維持原樣。ANE 只用於 ASR。

## 0.2.5
- **「可能是同一人」不再列出兩個都還沒命名的語者**：原本連「我NN ↔ 對方NN」這種雙方都是自動標籤的也列出來 → 都還沒命名根本無從判斷、又一大堆雜訊。改成只在**至少一方有真名**時才建議（才能判斷某個匿名聲紋是不是某位已命名的人）。

## 0.2.4
- **沒有可試聽片段就不顯示「🔊 試聽」**：語者沒有任何「有時長」的發言段時（例如 live final 沒有結束時間），原本仍顯示試聽鈕、點了才跳「沒有可試聽的音訊片段」。`/speakers` 現在回傳每位 `has_sample`，沒有就不顯示該鈕。

## 0.2.3
- **修正語者管理頁出現重複列**：一個名字可能對應多個聲紋（把某人改名成已存在的名字、或各自登記後命名相同），頁面原本是**一個聲紋一列**、但統計卻是**依名字**算 → 同名聲紋變成多列且數字一模一樣，被改名走的舊名字則消失。改成**依名字分組（一個人一列）**，並顯示該名字底下的聲紋數；改名／刪除改為**依名字**操作（套用到該名字的所有聲紋與逐字稿）。「可能是同一人」建議也略過同名、去除重複配對。

## 0.2.2
- **不同人說話強制斷句**：分群時，若一句辨識結果橫跨兩位以上說話者的輪次，會在語者切換處**切開成多行**，每個人各自一行（依各輪佔的時間比例切文字、並對齊到最近的標點，讀起來是完整句）。原本是整句歸給「佔比最多」的那位 → 會把別人的話算到同一人身上。單一說話者的句子不受影響。對該場按「多人分群」即套用。

## 0.2.1
- **語者試聽**：語者管理頁與「可能是同一人」建議都加上「🔊 試聽」— 抽該語者最長的一段發言、從會議音訊即時切出來播，不用先進會議聽也能判斷是誰／是否同一人。新增 `GET /speakers/{name}/sample.wav`。

## 0.2.0
- **持久化多人分群（跨會議語者庫）**：把既有的跨會議聲紋辨識補上完整的使用者層 + 管理 + 穩健度。
  - **全域語者管理頁**（首頁「👥 語者」／`/speakers/manage`）：列出所有記住的語者（名稱、出現會議數、發言數）；**全域改名**（套用到所有會議的逐字稿 + 聲紋）、**合併同一人**（兩個聲紋指向同一人）、**刪除聲紋**。
  - **跨會議語者檢視**：點某位語者 →「查看發言」列出他在所有會議的發言（依會議分組、可跳到該場）。
  - **預設開啟 + 顯示認得的人**：跨會議辨識預設啟用；會議頁顯示「🔁 本場認得：…」（本場出現、且跨會議認得的語者）。
  - **全域開關**：語者頁可一鍵關閉「跨會議記住說話者」，關閉後分群只在單場內、不建立／比對全域聲紋。
  - **辨識穩健度**：cosine 門檻可調（預設 0.62，越高越保守）；自動偵測並提示「可能是同一人」的近似聲紋，一鍵合併；聲紋隨多場發言滑動平均（漂移收斂）。

## 0.1.46
- **進度已知時就顯示真進度條(不再用循環動畫)**：分群等工作一開始 total 未知 → 卡片建成不定式動畫;但拿到 total(如 27/1361)後沒換成真進度條,一直循環。改成進度浮層的重建判斷把「是否已知 total」也算進去,工作從不定式→確定式時重建成**真實百分比進度條**(之後就地平滑更新)。事件比對的識別碼仍只用 mid/kind,不會誤判成「完成」。

## 0.1.45
- **修正亂偵測到會議(背景 app 誤判)**：`/detect`(HUD／橫幅用)原本是「麥克風使用中 **或** 有已知會議 app 在跑」就判定開會 → 但 Teams/Slack 等整天常駐背景,光憑行程就誤報(實測:沒開麥克風卻跳「MSTeams 進行中」)。改成有 micbusy 偵測器時**以麥克風為準**(app 名只拿來標示),只有在沒有 micbusy 時才退回行程判斷。`meeting_watch.py` 的通知迴圈本來就這樣判,現在 `/detect` 對齊。

## 0.1.44
- **Live 即時預覽改字不再那麼頻繁**：interim(暫定字)原本每 ~0.4–0.6 秒就重辨識尾段、改寫一次預覽 → 字一直跳、浪費算力,且每次重辨識成長中的尾段可能把字越改越錯。把預覽改寫節奏放慢到約**每 1.2 秒**(自適應下限 0.4→**1.0 秒**),最多約 1 次/秒。預覽只是暫定,結束斷句的 final 才是準的;可用 `LIVE_INTERIM_S` 調整。

## 0.1.43
- **修正全域進度浮層一直閃**：浮層原本每次輪詢(1.5 秒)就整個 `innerHTML` 重建 → 進入動畫(`pgin` 滑入)每次重跑、不定式進度條每次重啟 = 視覺一直閃。改成只有「工作集合改變」時才重建 DOM(進入動畫只跑一次),否則就**就地更新**進度條寬度／百分比／文字(寬度平滑過渡、動畫不中斷)。Live 進行中的「🔴 即時辨識中」卡照常顯示,只是不再閃。

## 0.1.42
- **原生浮動會議偵測 HUD（Notion 風）**：新增 `hud.py` — PyObjC 原生 always-on-top 浮動視窗，釘在螢幕底部置中。偵測到會議（麥克風使用中／已知會議 app）且未錄音時跳出「📝 偵測到會議 · <app> 進行中 — 開始轉譯」+「開始轉譯」鈕（點了開 /live），✕ 可關到本次通話結束；偵測流在背景執行緒、不卡 UI；浮層可跨桌面／全螢幕 app。`meeting_watch.py` 在有 PyObjC 時自動改用 HUD，否則退回原本的系統通知（`MEETING_HUD=0` 可強制關閉）。bootstrap 會盡力安裝 `pyobjc-framework-Cocoa`。

## 0.1.41
- **修正分群時整個介面卡住、無法切換頁面**：分群雖然丟到背景執行緒，但 sherpa 的 `OfflineSpeakerDiarization.process()` 是 C++ 且**不釋放 GIL**（實測：分群進行中主執行緒 2 秒只跑 1 次、卡 2145ms）→ 整個 Python 直譯器凍結，連 asyncio 事件迴圈都停 → 伺服器無法回應其他頁面。執行緒救不了 GIL-bound 的 C 呼叫，改成跑在**獨立子行程**（自有 GIL，父行程只在 `queue.get()` 等待＝會釋放 GIL），進度照樣經 queue 回傳。實測：分群進行中主執行緒可自由運行（最大停頓 8ms）、154 段進度正常回報。註：MLX（辨識／摘要）本來就會釋放 GIL，不受影響。

## 0.1.40
- **Live 即時辨識也納入全域進度顯示**：`/jobs` 原本只含背景工作（重新辨識／分群／摘要），**正在錄音中的即時辨識沒被列入** → 離開 Live 頁就看不到。新增 `live_active` 追蹤進行中的錄音，`/jobs` 多回一張「🔴 即時辨識中」卡，任一頁的全域進度浮層都看得到（錄音停止後自動消失、首頁列同步更新）。（重新辨識／上傳的辨識進度本來就已在 `/jobs`；若仍看不到，多半是執行中 app 還沒更新。）

## 0.1.39
- **減少會議記錄瞎掰（杜撰負責人／時間）**：(1) 強化提示詞 — 明確禁止寫出逐字稿沒出現過的人名（點名 小明/小米 這類），行動項目格式直接內建「沒明說就寫 未指定／未定」。(2) 新增**確定性接地檢查 `_ground`**：摘要指派給 負責人／期限／時間／日期 的值，只要**逐字稿裡完全沒出現過**就是模型編的 → 自動換成「未指定／未定」（含 `- [小米] …` 的括號負責人）。真的在會議裡講過的名字／時間會出現在逐字稿，故予保留。map-reduce 的每個分段也先接地，避免分段的杜撰滲進最終摘要。

## 0.1.38
- **進度浮層改全域 + 進度條更直觀**：進度卡從「只在會議頁」改成**每一頁都有**（首頁／Live／設定／會議頁），由單一全域輪詢器讀 `/jobs` 驅動。可**同時堆疊多項**工作（每場錄音一張卡：種類標籤＋會議標題＋粗進度條＋大字 %；無總量的摘要顯示流動式動畫條）。各頁透過 `meetingjobs` 事件反應（首頁列上標記＋完成自動刷新；會議頁完成時重載／就地渲染摘要），不再各自輪詢。`/jobs` 補上會議標題。

## 0.1.37
- **修正摘要整段重複（同一行印 N 次）**：根因是 `repetition_penalty` 的視窗 `repetition_context_size` 預設只有 **20 tokens**，比一個清單項目還短 → 行一重複時，前面相同的 token 已滑出視窗、罰不到 → 整行無限迴圈。修正：(1) 視窗放大到 **512**、罰則 1.3→**1.5**，並加 `frequency_penalty`（依出現次數加重）；(2) 加**輸出後處理**：折疊連續重複的清單行（比對時去掉開頭編號/符號），不論模型或罰則有沒有壓住都保證不會印出整排重複。

## 0.1.36
- **修正部分瀏覽器（Safari）收不到會議偵測通知**：`Notification.requestPermission()` 原本在頁面載入時就呼叫，但 Safari（Chrome 也不穩）只在**使用者手勢**中才允許要求權限 → 載入時呼叫被靜默忽略，權限永遠停在 `default`，通知分支不會觸發（所以有些瀏覽器有、有些沒有）。改成在**第一次點擊／按鍵**時才要求權限。註：通知權限是 per-origin（含 port），.app（8765）上授權不會帶到不同 port 的 dev。

## 0.1.35
- **修正用 Qwen3-ASR(mlx) 上傳音檔崩潰／無逐字稿**：(1) mlx-audio 載入音檔會呼叫 **ffprobe**，只裝 ffmpeg（沒 ffprobe）時直接 `RuntimeError: ffprobe not found` → 上傳 500。改成 backend 一律用 **ffmpeg** 自行解碼成 16k 單聲道 PCM 再餵 mlx-audio（不傳檔案路徑、不需 ffprobe）。(2) Qwen3-ASR 沒有長音訊內部切段，整段長檔丟進去會回空（3 分鐘檔→空逐字稿）。改成上傳的長音訊在 backend 內**切成 ~30 秒視窗**逐段辨識並帶時間戳。live／重新辨識的短輸入維持單段不變。

## 0.1.34
- **首頁顯示哪些錄音正在處理中**：新增 `/jobs` 彙整所有背景工作（辨識／分群／摘要，含上傳中的辨識）。首頁每 2.5 秒輪詢，在該場會議列加上「⏳ 辨識 60%」即時標記、頂部顯示「⏳ N 場處理中」；某場完成或有新場開始處理時自動重抓清單（更新標題／狀態）。

## 0.1.33
- **全功能非同步化 + 共用進度浮層**：摘要產生（LLM，本來會卡住連線數秒~數分鐘）改為背景工作 + 進度輪詢，POST 立即返回、完成就地渲染（不重整）。三個重活（語音辨識／多人分群／摘要）統一走**同一個泛用輪詢器 `pollJob`**＋**右下角浮動進度卡 `Prog`**：有總量顯示百分比進度條，沒有（摘要）顯示不定式動畫條；重整可續、只對自己啟動/觀察到的工作動作（不會被殘留的 done 誤觸）。移除各按鈕旁分散的行內狀態字。模型下載／runtime 編譯本來就已非同步。

## 0.1.32
- **多人分群顯示進度**：分群改為非同步背景工作，沿用 re-transcribe 的進度輪詢機制。sherpa `OfflineSpeakerDiarization.process` 本來就支援 chunk 進度 callback，現在把它接到 `diarize_pcm(on_progress=…)` → 會議頁即時顯示「分群中 N%」（多軌會標 `[1/2]`），重整可續、完成自動刷新。POST 立即返回（不再卡著連線到分群結束）。

## 0.1.31
- **修正 femelo 安裝出現 `cannot import name '_py_qwen3_asr_cpp'`**：`py-qwen3-asr-cpp` 在 PyPI 沒有對應 (python, arm64) 的 wheel，一律是原始碼編譯（scikit-build/cmake，需 cmake + C/C++ 工具鏈）。缺工具鏈時會裝進 python 檔案卻沒編出原生模組 `.so`，且一旦半套安裝卡住，之後 `pip install` 會視為「已滿足」而不再重編，無法自癒。修正：femelo 路徑現在(1) 驗證**真正的** model import；(2) 失敗時自動 `brew install cmake`、檢查 Xcode CLT；(3) **先卸載再 `--force-reinstall --no-cache-dir` 乾淨重編**；(4) 仍失敗則給明確指引（裝 `xcode-select --install` + cmake）。先前只有 chatllm 會自動裝 cmake，femelo 沒有。

## 0.1.30
- **修正上傳音檔可辨識卻「無法播放」**：上傳的單一檔案應從 0 秒開始，但 PCM 解碼段是用「解碼完成時間」(`time.time()`) 當 `started_at` 註冊的；解碼是在整段 transcribe 跑完後才做（可能數分鐘），導致 `_assemble_track` 在音訊前面墊了數分鐘靜音（實測一場墊了 4.6 分鐘），播放像「沒聲音」、且與 0 起算的逐字稿時間軸對不上。改成錨定在會議的 `created_at`（偏移 0）。

## 0.1.29
- **上傳音檔改非同步**：上傳後不再卡著 HTTP 連線等整個 transcribe＋summary（數分鐘、瀏覽器空轉、還會被 supervisor 當成 hang 殺掉）。改成存檔→建會議→**背景執行緒**跑辨識＋摘要＋自動標題，POST 立即 303 轉址到該會議頁。會議頁沿用 re-transcribe 既有的進度輪詢（重整可續、完成自動刷新）。背景工作直接辨識原始檔（mlx-whisper 吃 m4a/wav/mp3），不依賴 best-effort 的 ffmpeg PCM 解碼（解碼僅供回放）。移除已無用的同步結果頁。

## 0.1.28
- **修正 Qwen3-ASR (mlx) 把 `STTOutput(text='', segments=[...])` 整串存進逐字稿**：靜音段的 `STTOutput.text` 是合法的空字串，但舊的取值 `getattr(r,'text',None) or str(r)` 遇到空字串會 falsy → 退而 `str(r)` 變成整個物件的 repr。改成空字串照樣回空（下游正常丟棄）。對受影響的場次按「重新語音辨識」即可清掉。

## 0.1.27
- **live interim 節奏依算力自動調整（鎖定 duty cycle ~75%）**：macOS 拿不到非 sudo 的 GPU 使用率，但 interim 重辨識的 RTF（compute / 即時）就是 GPU-bound MLX pipeline 的有效負載。每次 interim 計時後，把下次間隔設為 `compute / 目標duty`（夾在 0.4~3.0s）：快模型自動更頻繁（吃滿閒置算力）、慢模型（1.7B）自動拉長間隔不再積壓拖垮 live。沿用 `AdaptiveBackend` 的同套 RTF 訊號。目標 duty 預設 0.75，可用 `LIVE_INTERIM_DUTY` 調。

## 0.1.26
- **錄音時降低會議偵測輪詢頻率**：每頁的 `/detect` 輪詢（會跑 `ps` 找會議 app + CoreAudio 查麥克風）在「錄音中」根本沒用（偵測橫幅只在「未錄音」時提示開始錄音），卻仍每 10 秒打一次。改成自我排程：未錄音維持 10 秒、錄音中拉長到 30 秒，省下 live 進行時無謂的系統呼叫。註：live 的真正算力在 interim 重辨識（每 0.6 秒重跑 8 秒尾段），1.7B 才會吃不消；VAD 偵測本身極輕、非瓶頸。

## 0.1.25
- **變更紀錄 + 摘要支援 Markdown 顯示**：標題 / **粗體** / `code` / 條列 / 編號清單渲染成 HTML（之前顯示原始 `#` `**` `-`）。極簡、離線自包含的 md→HTML（伺服器端 + 重新摘要前端同步渲染、會 HTML-escape 防注入），無 CDN。

## 0.1.24
- **修正 Qwen3-ASR 1.7B（mlx）在 live / 重新辨識崩潰**：MLX 的 GPU stream 是 thread-local，模型在某 thread 載入、卻在另一 thread（live threadpool / 重新辨識背景 thread）執行 → `no Stream(gpu, 1) in current thread` 崩潰。改成所有 mlx-audio 工作（載入＋每次推論）固定在**單一專屬 thread**，並於每次推論後 `clear_cache` 降峰值記憶體。重新辨識實測 30/30 通過。
- 註：1.7B 權重常駐約 2.5GB；記憶體吃緊的 Mac 同時跑 live（interim whisper + 1.7B + 摘要）仍可能 OOM。**1.7B 建議用於批次精校；live 用較輕模型（turbo-q4 / 0.6b / whisper-small）。**

## 0.1.23
- **Qwen3-ASR 1.7B Metal 加速（mlx-audio）**：改用 `mlx-audio` 載入 mlx-community 官方轉檔（8bit），in-process Metal，**RTF 0.13（比 chatllm GGUF 的 0.61 快約 4.6x）**，正確 zh、輸出轉繁體。live 與重新辨識可選、模型管理可下載。chatllm 1.7B 保留為備用。捨棄會跑社群個人自訂程式碼的 mlx-vlm 路線（改用維護中、社群信任的 mlx-audio + mlx-community 權重）。

## 0.1.22
- **重複迴圈防護改用「壓縮率」通用判據**（最佳解）：迴圈的壓縮率遠高於自然語音（實測自然 zh ~0.9、每日×N ~4.2、喬×N ~8.6，門檻 1.8 留大 margin），不挑前綴、不挑引擎都抓得到 — 含**無重複懲罰參數的 femelo 預設引擎**。live 與批次（重新辨識）都套用，取代原本只看「相異字數」會被前綴騙過的判定（「我們每日每日…」漏掉的那種）。

## 0.1.21
- **斷句門檻 400→500ms**（沒那麼激進）：實測同一段（silero VAD）350ms→12 段/4 個破碎短句、450~500ms→8 段/1 個、800ms→0 但 finalize 慢 +400ms。500ms 在「不切壞句子」與「不延遲」之間最佳，單句不再被輕微停頓硬切。可用 `LIVE_SILENCE_MS` 或 live 的辨識單元調整。

## 0.1.20
- **live 斷句改用 silero 神經 VAD 為預設**（原本能量 RMS）：能量 VAD 把句尾輕音當靜音 → 提早切斷、掉字；silero 精準抓語音邊界（實測語音 86/99、靜音 0/66 命中）。live 可選「能量」退回；模型缺失自動退回能量。ASR 算力本就不是瓶頸（mlx Metal RTF 0.027），斷句才是 live 體感關鍵。

## 0.1.19
- **歷史管理介面大改**：會議列表改為**依日期分組**（今天／昨天／N 天前／日期）的資訊列 — 顯示時間、時長、狀態、標籤；滑過列即可**快速刪除**。新增**選取模式**：勾選多場一次**刪除**或**合併**。搜尋＋標籤篩選保留。`/meetings` 補上 `duration_s`／`n_segments`。

## 0.1.18
- **摘要自動排除不合理用詞 + 統一繁體**：(1) 餵給摘要的逐字稿先濾掉辨識幻覺（優悠獨播劇場…），摘要不再複述雜訊；(2) 摘要輸出統一轉**繁體（台灣）**（LLM 常吐簡體）；(3) 提示詞要求忽略亂碼／影片字幕類內容。幻覺黑名單強化：TV／字幕片頭片語改**子字串比對**（不受前後填充字影響）。

## 0.1.17
- **修正空白/靜音音訊的 whisper 幻覺**（優優獨播劇場 / YoYo Television Series / 謝謝觀看 等）：批次「重新辨識」現在也套用幻覺黑名單（原本只有 live 有），並補上 whisper-zh 常見的靜音幻覺片語。對該場按「重新辨識」即可清掉已存的幻覺。

## 0.1.16
- **修好會議偵測**：(1) 背景 watcher 在 supervise 下沒有重啟，死一次整場就沒偵測 → 加存活重啟。(2) osascript 通知常被 macOS 通知權限／專注模式靜音、根本看不到 → 改在**網頁 UI 顯示偵測橫幅 + Web Notification**（PWA，可靠）。新增 `/detect`；任一頁面每 10 秒輪詢，偵測到會議（麥克風使用中／已知會議 app）且未錄音時，跳頂部橫幅＋瀏覽器通知，可一鍵開始錄音。

## 0.1.15
- **修正會議頁所有按鈕失效**（重新辨識/分群/匯出/複製…全壞）：0.1.12 加的單行 `//` 註解把整段 JS 後半註解掉導致 SyntaxError。移除註解；加回歸測試禁止單行腳本中的 `//` 註解。

## 0.1.14
- **修正摘要瞎掰**：摘要改**貪婪解碼**（temp 0）+ 重複懲罰，只用逐字稿真實資訊（實測改用逐字稿出現的真實人名，缺的寫「待定/無」），不再杜撰 小明/小紅/小黃。
- **ASR 重複迴圈防護**：整段重複（喬喬喬…×N）直接丟棄、正常重疊（哈哈/好的）收斂；批次 whisper 加 `condition_on_previous_text=False` + 壓縮率門檻，避免迴圈往後擴散。

## 0.1.13
- UI 收尾：把瀏覽器原生最醜的元件（檔案選擇鈕、下拉箭頭）改成與設計系統一致的樣式（自訂 chevron、檔案鈕做成 chip、輸入框 hover）。維持離線自包含（不外連 CDN）。

## 0.1.12
- 「實驗性功能」集中到**設定頁**（新增區段 + 導覽），不再內嵌在各會議頁。分群重疊標記改由設定開關控制（localStorage 記住），多人分群時生效。

## 0.1.11
- **跨會議說話者辨識**：分群時把每位說話者的聲紋（3D-Speaker embedding）存進全域語者庫，下次會議**自動認出同一個人並沿用名稱**；在某場把「對方N」改成真實姓名，會套用到之後認出的場次。不同聲音建立新身份（保守 cosine 門檻 0.62，寧可分開也不誤認）。可在分群選項關閉（`enroll`）。
- 修正「⋯ 更多」選單往右展開（不再偏左被切到）。

## 0.1.10
- **標籤分類**：會議可加標籤（#客戶 / #1on1 / #產品…），首頁標籤列點選篩選，詳情頁標籤增刪（多對多）。
- **摘要操作收進「⋯ 更多」下拉**：只留「產生摘要」為主按鈕，多人分群 / 匯出 / 複製 / 完成 / 刪除 收進選單，按鈕列不再爆滿。
- **修正摘要杜撰**：強化 prompt 禁止編造人名 / 日期 / 期限，逐字稿沒提到的負責人寫「未指定」、期限寫「未定」。

## 0.1.9
- **跨會議搜尋**：首頁搜尋框，搜標題 / 逐字稿 / 摘要（CJK 子字串），點結果直接進該會議。
- **匯出 / 複製**：會議詳情可匯出 Markdown（標題+摘要+時間戳逐字稿）、一鍵複製摘要 / 複製逐字稿。
- **行動項目摘要**：摘要型式新增「行動項目」— 抽 行動項目（誰/做什麼/期限）、決議、待解問題。
- **修正語言設定失效**：femelo（.cpp 0.6B）會**忽略語言設定**，短句常誤判成日文。現在指定語言時自動改用 whisper（turbo-q4，確實鎖定語言；auto 模式仍用 femelo）。

## 0.1.8
- 🧪 實驗性功能（摘要區下方摺疊區）：分群時**標記重疊/多人語音** — 同一句偵測到 2 位以上說話者會標「🔀 說話者A+B」。啟發式（sherpa 把同時說話 collapse 成單人，這是從分群片段推估，非真分離），預設關閉。

## 0.1.7
- 產生摘要時**自動產生標題**：上傳或重新摘要後，依摘要內容產生標題（僅在標題仍為預設時套用，不覆蓋手動命名）。
- 原生 App **快速跳轉**：服務已在執行時再次開啟 App，直接開頁面、不重跑安裝。
- 修正版本發佈 CI：**先發佈安裝檔**（installer 一定上架），chatllm 預編譯改為限時非阻塞 + 建置快取，且只編譯 `libchatllm`（不再整包編譯，數十分鐘 → 數分鐘）。

## 0.1.6
- chatllm 加速 runtime 改 **CI 預編譯**：設定→加速 runtime 一鍵安裝會先下載可攜 arm64 bundle（rpath 改 `@loader_path` + libggml 同目錄），**免 cmake**；下載不到才原始碼編譯（自動 `brew install cmake`）。
- femelo 不再綁 python3.14：接受**任何 python≥3.11**（找不到才 `brew install python@3.13`），改用 PyPI wheel 安裝。
- `package_chatllm.sh`：產生可攜 chatllm runtime tar，release 自動附上。

## 0.1.5
- 「模型管理」重新設計為「設定」頁：分三區（系統·更新 / 模型 / 加速 runtime）+ 區段導覽 + 版本顯示，更新/釋放記憶體整併到系統區。

## 0.1.4
- 加速 runtime 加上「清除」：移除該 runtime 的編譯目錄（femelo→`.venv-qwen314`、chatllm→`chatllm.cpp`），狀態回到未安裝；重新編譯可復原，模型權重保留。

## 0.1.3
- 統一按鈕大小：移除首頁 Live 按鈕的特例尺寸，表單列改 `align-items:end`，輸入框與按鈕底邊對齊，不再有大有小。
- 更新改為**手動決定**：啟動不再自動套用更新，只在模型管理頁「檢查更新 → 更新並重啟」由使用者決定。

## 0.1.2
- 修正說話者分群：sherpa 模型（pyannote-3-0 切割 + 3D-Speaker 聲紋）首次分群**自動下載**到 `models/`，乾淨機器免手動設定。
- 移除 community-1 onnx 選項（與 sherpa 不相容，缺 `sample_rate` metadata）。
- 模型下載 / runtime 編譯失敗時，於模型管理頁顯示明確錯誤（不再只進伺服器 log）。
- 新增 README 與 App 內變更紀錄。

## 0.1.1
- CD：tag `v*` 於 macOS runner 編譯 `.app` 並附到 Release。
- 手動「檢查更新」：模型管理頁比對 GitHub Releases → 更新並重啟。
- 共用 `updater.py`（bootstrap 與 App 共用）。

## 0.1.0
- 首個版本：Live 即時逐字稿（兩段式）、會後精校、說話者分群、摘要、雙軌回放。
- 原生 `.app`（首次啟動安裝相依 + 依需求下載模型）、PWA、UI 內關閉。
- 開會偵測通知；標題 / 說話者命名；辨識語言指定。
- 加速 runtime：femelo（0.6B）、chatllm（1.7B），Metal。
- 服務綁 `127.0.0.1:8765`（隱私優先）。

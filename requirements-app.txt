# Core runtime for the packaged .app (must install — server won't run without these).
# mlx-whisper/mlx-lm are installed SEPARATELY, best-effort (Apple Silicon only;
# skipped if they fail so the app still runs). The .cpp Metal runtimes (femelo/
# chatllm) install from the 模型管理 編譯安裝 button.
numpy
opencc
fastapi
uvicorn[standard]
python-multipart
huggingface_hub
sherpa-onnx
onnxruntime
